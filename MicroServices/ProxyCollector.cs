namespace SB_Parser_API.MicroServices
{
    public class ProxyCollector
    {
        static void proxyCollector()
        {
            //ProcessInit("Proxy collector-inspector");

            //int pid = 0; //RegMe(0);
            //Console.WriteLine($"pid={pid}");
            //if (pid < 0) Environment.Exit(pid);  //Environment.FailFast($"Critical error №{pid}!!!");  Надо дописать обработку позже.

            //get_hidemy_name_list();

            Console.ReadLine();
        }
    }
}
